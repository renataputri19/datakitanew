/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/logs/*.log
/vendor
.env
.env.backup
.env.production
.env.local
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
/.refact
.DS_Store
Thumbs.db

# Database dumps must NEVER be committed — they contain real user data and
# password hashes. The Docker seed (docker/seed/) is the one intentional
# exception and is not matched by this root-only pattern.
/*.sql
